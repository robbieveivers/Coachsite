---
interface Props {
  title: string;
}

const { title } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Professional Ironman and triathlon coaching with Chelsea Seymour" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <script is:inline async src="https://www.instagram.com/embed.js"></script>
  </head>
  <body>
    <header>
      <nav>
        <a href={`${base}/`}>Home</a>
        <a href={`${base}/about`}>About</a>
        <a href={`${base}/contact`}>Contact</a>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <div class="footer-content">
        <div class="footer-section">
          <h3>Connect With Me</h3>
          <div class="social-links">
            <a href="https://youtube.com/@chels.seymour" target="_blank" rel="noopener noreferrer" title="YouTube">
              🎥 YouTube
            </a>
            <a href="https://instagram.com/chels.seymour" target="_blank" rel="noopener noreferrer" title="Instagram">
              📷 Instagram
            </a>
            <a href="https://facebook.com/chelseaseymourcoaching" target="_blank" rel="noopener noreferrer" title="Facebook">
              👥 Facebook
            </a>
          </div>
        </div>
        <div class="footer-section">
          <h3>Contact</h3>
          <p>📧 <a href="mailto:chelsea@example.com">chelsea@example.com</a></p>
          <p>📞 <a href="tel:+1234567890">+1 (234) 567-890</a></p>
        </div>
        <div class="footer-section">
          <p>&copy; 2025 Chelsea Seymour Coaching. All rights reserved.</p>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  :root {
    --accent: #ec4899;
    --accent-dark: #be185d;
    --accent-light: #f9a8d4;
    --black: #13151a;
    --gray: #3f3f46;
    --gray-light: #fdf2f8;
    --gray-lighter: #fef3f8;
    --white: #ffffff;
    --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.12), 0 1px 2px rgba(0, 0, 0, 0.06);
    --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.1), 0 2px 4px rgba(0, 0, 0, 0.06);
    --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1), 0 4px 6px rgba(0, 0, 0, 0.05);
  }
  
  * {
    box-sizing: border-box;
  }
  
  html {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: var(--white);
    color: var(--black);
    line-height: 1.6;
  }
  
  body {
    margin: 0;
    padding: 0;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }
  
  header {
    background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%);
    color: var(--white);
    padding: 1.5rem;
    box-shadow: var(--shadow-md);
    position: sticky;
    top: 0;
    z-index: 100;
  }
  
  nav {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    gap: 2rem;
    align-items: center;
  }
  
  nav a {
    color: var(--white);
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    padding: 0.5rem 1rem;
    border-radius: 4px;
  }
  
  nav a:hover {
    background: rgba(255, 255, 255, 0.2);
    transform: translateY(-2px);
  }
  
  main {
    flex: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
    width: 100%;
  }
  
  footer {
    background: linear-gradient(135deg, var(--gray-light) 0%, var(--gray-lighter) 100%);
    padding: 3rem 2rem;
    color: var(--gray);
    border-top: 3px solid var(--accent);
  }
  
  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    text-align: left;
  }
  
  .footer-section h3 {
    color: var(--accent);
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 1.2rem;
  }
  
  .footer-section p {
    margin: 0.5rem 0;
  }
  
  .social-links {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  
  .social-links a {
    color: var(--accent);
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    display: inline-block;
    padding: 0.5rem;
    border-radius: 4px;
  }
  
  .social-links a:hover {
    background: rgba(236, 72, 153, 0.1);
    transform: translateX(5px);
  }
  
  footer a {
    color: var(--accent);
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
  }
  
  footer a:hover {
    color: var(--accent-dark);
    text-decoration: underline;
  }
  
  @media (max-width: 768px) {
    nav {
      flex-direction: column;
      gap: 1rem;
    }
    
    nav a {
      text-align: center;
    }
    
    .footer-content {
      grid-template-columns: 1fr;
      text-align: center;
    }
    
    .social-links {
      align-items: center;
    }
  }
</style>
