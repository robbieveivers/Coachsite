---
import Layout from '../layouts/Layout.astro';
const base = import.meta.env.BASE_URL;
---

<Layout title="Chelsea Seymour - Ironman & Triathlon Coaching">
  <div class="hero">
    <img src={`${base}/hero-triathlon.svg`} alt="Triathlon Coaching" class="hero-image" />
    <div class="hero-content">
      <h1>Chelsea Seymour Coaching</h1>
      <p class="hero-subtitle">Transform Your Ironman & Triathlon Journey</p>
    </div>
  </div>
  
  <section class="intro">
    <p class="intro-text">
      Hi! I'm Chelsea Seymour, and I'm passionate about helping athletes conquer their 
      Ironman and triathlon goals. Whether you're training for your first sprint triathlon 
      or targeting that Ironman finish line, I'm here to guide you every stroke, pedal, 
      and stride of the way.
    </p>
  </section>

  <section class="coaching-gallery-section">
    <h2 class="section-title">Coaching in Action</h2>
    <div class="coaching-gallery">
      <div class="gallery-item">
        <img src={`${base}/images/coaching/coaching-1.jpg`} alt="Coach Chelsea Seymour working with triathlon athletes during training session" class="gallery-image" loading="lazy" />
      </div>
      <div class="gallery-item">
        <img src={`${base}/images/coaching/coaching-2.jpg`} alt="Personalized triathlon coaching and technique analysis" class="gallery-image" loading="lazy" />
      </div>
      <div class="gallery-item">
        <img src={`${base}/images/coaching/coaching-3.jpg`} alt="One-on-one coaching sessions for Ironman and triathlon preparation" class="gallery-image" loading="lazy" />
      </div>
    </div>
  </section>

  <section>
    <h2 class="section-title">Coaching Services</h2>
    <div class="services">
      <div class="service-card">
        <img src={`${base}/service-swim.svg`} alt="Swimming" class="service-image" />
        <div class="service-content">
          <h3>🏊‍♀️ Swim Technique Excellence</h3>
          <p>Specialized swim coaching with video analysis and technique refinement to help you glide through the water efficiently.</p>
        </div>
      </div>
      <div class="service-card">
        <img src={`${base}/service-bike.svg`} alt="Cycling" class="service-image" />
        <div class="service-content">
          <h3>🚴‍♀️ Personalized Training Plans</h3>
          <p>Custom Ironman and triathlon training programs tailored to your lifestyle, fitness level, and race goals.</p>
        </div>
      </div>
      <div class="service-card">
        <img src={`${base}/service-run.svg`} alt="Running" class="service-image" />
        <div class="service-content">
          <h3>🏃‍♀️ Race Day Mastery</h3>
          <p>Complete race preparation including pacing strategy, nutrition planning, and mental preparation for your big day.</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <h2>Ready to Get Started?</h2>
    <p>Let's work together to achieve your triathlon goals!</p>
    <p>
      <a href={`${base}/contact`} class="cta-button">Contact Me Today</a>
    </p>
  </section>
</Layout>

<style>
  .hero {
    position: relative;
    margin: -2rem -2rem 2rem -2rem;
    height: 500px;
    overflow: hidden;
    border-radius: 0 0 20px 20px;
    box-shadow: var(--shadow-lg);
  }
  
  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  
  .hero-content {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    text-align: center;
    color: var(--white);
    width: 90%;
    max-width: 800px;
    text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
  }
  
  .hero-content h1 {
    font-size: 3.5rem;
    margin: 0 0 1rem 0;
    color: var(--white);
    font-weight: 800;
  }
  
  .hero-subtitle {
    font-size: 1.8rem;
    font-weight: 500;
    margin: 0;
  }

  .intro {
    background: linear-gradient(135deg, var(--gray-light) 0%, var(--gray-lighter) 100%);
    padding: 2.5rem;
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    margin: 2rem 0 3rem 0;
  }
  
  .intro-text {
    font-size: 1.2rem;
    line-height: 1.8;
    margin: 0;
    text-align: center;
    color: var(--gray);
  }

  section {
    margin: 3rem 0;
  }
  
  .section-title {
    text-align: center;
    color: var(--accent);
    font-size: 2.5rem;
    margin-bottom: 2.5rem;
    font-weight: 700;
  }

  .services {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2.5rem;
    margin-top: 2rem;
  }

  .service-card {
    background: var(--white);
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    overflow: hidden;
    transition: all 0.3s ease;
    border: 2px solid var(--gray-light);
  }
  
  .service-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
    border-color: var(--accent-light);
  }
  
  .service-image {
    width: 100%;
    height: 200px;
    object-fit: cover;
  }
  
  .service-content {
    padding: 2rem;
  }

  .service-card h3 {
    color: var(--accent);
    margin-top: 0;
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }
  
  .service-card p {
    color: var(--gray);
    line-height: 1.7;
  }
  
  .cta-section {
    text-align: center;
    background: linear-gradient(135deg, var(--accent-light) 0%, var(--accent) 100%);
    padding: 3rem;
    border-radius: 16px;
    box-shadow: var(--shadow-lg);
    margin: 4rem 0;
  }
  
  .cta-section h2 {
    color: var(--white);
    font-size: 2.5rem;
    margin-top: 0;
    margin-bottom: 1rem;
  }
  
  .cta-section > p {
    color: var(--white);
    font-size: 1.3rem;
    margin-bottom: 2rem;
  }

  .cta-button {
    display: inline-block;
    background: var(--white);
    color: var(--accent);
    padding: 1.2rem 2.5rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 700;
    font-size: 1.2rem;
    transition: all 0.3s ease;
    box-shadow: var(--shadow-md);
  }

  .cta-button:hover {
    background: var(--gray-lighter);
    transform: translateY(-3px);
    box-shadow: var(--shadow-lg);
  }

  .coaching-gallery-section {
    margin: 3rem 0;
  }

  .coaching-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .gallery-item {
    position: relative;
    overflow: hidden;
    border-radius: 12px;
    box-shadow: var(--shadow-md);
    transition: all 0.3s ease;
    aspect-ratio: 4 / 3;
    background: var(--gray-light);
  }

  .gallery-item:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
  }

  .gallery-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }

  .gallery-item:hover .gallery-image {
    transform: scale(1.05);
  }
  
  @media (max-width: 768px) {
    .hero {
      height: 350px;
      margin: -2rem -1rem 2rem -1rem;
    }
    
    .hero-content h1 {
      font-size: 2rem;
    }
    
    .hero-subtitle {
      font-size: 1.2rem;
    }
    
    .intro-text {
      font-size: 1rem;
    }
    
    .section-title {
      font-size: 2rem;
    }
    
    .services {
      grid-template-columns: 1fr;
    }
    
    .coaching-gallery {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }
</style>
